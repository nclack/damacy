# Run damacy_bench on the self-hosted GPU runner, convert results.json
# to the github-action-benchmark format, and publish to gh-pages so we
# can chart performance over time. Two scenarios run sequentially in one
# job (cheaper than a matrix: one docker build, dataset cache shared).
#
# Dashboard:  https://<owner>.github.io/<repo>/bench/
#
# Required one-time setup (see bench/README-tracking.md):
#   - gh-pages branch must exist
#   - Settings → Pages → source = gh-pages branch / root
#   - Settings → Actions → Workflow permissions = read & write
#     (or rely on the explicit `permissions:` block below)

name: bench

on:
  # Daily at 07:23 UTC (off the top of the hour so we don't compete with
  # GitHub's hourly cron surge). GitHub may delay scheduled runs by tens
  # of minutes under load — exactness doesn't matter for trend tracking.
  schedule:
    - cron: '23 7 * * *'
  workflow_dispatch:

permissions:
  contents: write       # auto-push commits to gh-pages
  pull-requests: write  # comment-on-alert
  deployments: write    # gh-pages deployment status

# Serialize against build / test as well — they share the self-hosted
# GPU runner, and a CUDA test running concurrently with the bench would
# contaminate the throughput measurements. All three workflows use the
# same `gpu-runner` group.
concurrency:
  group: gpu-runner
  cancel-in-progress: false

jobs:
  bench:
    runs-on: [self-hosted, gpu]
    # Cap the whole job at 30 min. Healthy run is ~2 min; a stuck
    # damacy_bench (see #20) used to sit until GHA's 6h default kicked
    # in or the user canceled, blocking the gpu-runner concurrency group.
    timeout-minutes: 30
    env:
      IMAGE: damacy:${{ github.sha }}-bench
      CONTAINER: damacy-bench-${{ github.run_id }}-${{ github.run_attempt }}
      # Persisted on the runner host so dataset gen (~2GB per scenario)
      # only happens once. bench/run.py only regenerates missing zarrs.
      BENCH_DATA: ${{ github.workspace }}/../damacy-bench-data

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build docker image (Release)
        run: docker build --tag "$IMAGE" .

      - name: Run bench scenarios + adapt to GHA-bench format
        # Healthy bench step finishes in ~1 min. Cap at 20 min so a
        # hung damacy_bench (#20) fails fast with logs intact instead
        # of riding the GHA default 6h or sitting until cancellation.
        timeout-minutes: 20
        env:
          # Prefix every metric with the runner name so histories from
          # different self-hosted machines stay separable on the dashboard.
          # ${{ runner.name }} is the runner's registered name in GitHub.
          BENCH_RUNNER: ${{ runner.name }}
        run: |
          mkdir -p "$BENCH_DATA"
          docker run --name "$CONTAINER" --device=nvidia.com/gpu=all \
            -e BENCH_RUNNER \
            -v "$BENCH_DATA":/workspace/damacy/bench/data \
            "$IMAGE" \
            bash -c '
              set -e
              cd /workspace/damacy
              for scenario in default mixed; do
                echo "::group::scenario=$scenario"
                # Inner per-scenario cap: a healthy scenario runs in
                # ~10s. SIGTERM at 5 min, SIGKILL 30s later if it does
                # not exit. Surfaces a hung damacy_bench scenario with
                # a non-zero rc and the partial stdout in the log.
                timeout --kill-after=30s 5m \
                  uv run bench/run.py "bench/scenarios/${scenario}.json" --no-report
                latest=$(ls -1d bench/runs/${scenario}/*/ | tail -1)
                uv run bench/to_gha_bench.py "${latest}results.json" \
                  --runner "${BENCH_RUNNER}" \
                  --out-smaller "/workspace/damacy/bench-${scenario}-smaller.json" \
                  --out-bigger  "/workspace/damacy/bench-${scenario}-bigger.json"
                echo "::endgroup::"
              done
              # Merge across scenarios — glob picks up every per-scenario
              # file the loop produced, so adding a scenario only requires
              # editing the loop above. The action takes one tool format
              # per call, so we publish smaller and bigger separately.
              uv run bench/merge_gha_bench.py \
                --out /workspace/damacy/bench-smaller.json \
                /workspace/damacy/bench-*-smaller.json
              uv run bench/merge_gha_bench.py \
                --out /workspace/damacy/bench-bigger.json \
                /workspace/damacy/bench-*-bigger.json
            '
          docker cp "$CONTAINER":/workspace/damacy/bench-smaller.json ./
          docker cp "$CONTAINER":/workspace/damacy/bench-bigger.json  ./
          docker rm "$CONTAINER"

      - name: Publish (smaller is better — timings)
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: damacy timings
          tool: customSmallerIsBetter
          output-file-path: bench-smaller.json
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: true
          gh-pages-branch: gh-pages
          benchmark-data-dir-path: timings
          # Loose threshold while we're collecting baseline noise data;
          # tighten once we have ~20 points and know the variance.
          alert-threshold: '150%'
          comment-on-alert: true
          fail-on-alert: false
          max-items-in-chart: 200

      - name: Publish (bigger is better — throughput)
        uses: benchmark-action/github-action-benchmark@v1
        with:
          name: damacy throughput
          tool: customBiggerIsBetter
          output-file-path: bench-bigger.json
          github-token: ${{ secrets.GITHUB_TOKEN }}
          auto-push: true
          gh-pages-branch: gh-pages
          benchmark-data-dir-path: throughput
          alert-threshold: '150%'
          comment-on-alert: true
          fail-on-alert: false
          max-items-in-chart: 200

      - name: Cleanup container
        if: always()
        run: docker rm -f "$CONTAINER" 2>/dev/null || true
